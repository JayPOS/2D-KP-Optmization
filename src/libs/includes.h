#ifndef __CONSTANTS
#define __CONSTANTS

#include "bits/stdc++.h"
#include <cstdlib>
#include <random>
#include <chrono>
#include <time.h>

using namespace std;

using f64 = double;
using f32 = float;
using i32 = long;
using i64 = long long;

using vi = vector<int>;
using ii = pair<int,int>;

const int HEAP_OVERFLOW = -1;
const int RANDOM = 3;
const int SEMI = 2;
const int NORMAL = 1;

#endif